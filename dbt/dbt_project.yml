name: 'strava_dbt'
version: '1.0.0'
config-version: 2

profile: 'strava_dbt'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  strava_dbt:
    materialized: view